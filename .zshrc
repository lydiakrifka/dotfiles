alias cg='oathtool --totp --base32 $(cat ~/.university-2fa)'
alias cg='oathtool --totp --base32 $(cat ~/.university-2fa)'
alias cg='oathtool --totp --base32 $(cat ~/.university-2fa)'

# Custom aliases
[ -f "$HOME/.aliases" ] && source "$HOME/.aliases"
